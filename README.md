# Imgur App for iOS

A native iOS app for uploading images from your photo library to **Imgur** and keeping track of your uploads.

> _“My first iOS app. Responsive to all devices. Easily upload images from your gallery to Imgur and keep track of your uploads.”_ – **Ferran Hendriks**

---

## ✨ Features

- 📸 **Upload from gallery**  
  Pick an image directly from your iOS photo library and upload it to Imgur.

- ☁️ **Imgur integration**  
  Uses the Imgur API to create anonymous uploads.

- 📂 **Upload history**  
  Keep track of previously uploaded images (e.g. links / metadata).

- 📱 **Responsive UI**  
  Designed to work across different iPhone screen sizes.

---

## 🏗 Project Structure

At the root:

- `ImgurApp.xcodeproj` – Xcode project file.
- `ImgurApp/` – Main iOS app source code.
- `ImgurAppTests/` – Unit tests.
- `ImgurAppUITests/` – UI tests.
- `.gitattributes` – Git attributes for the repo.

Open `ImgurApp.xcodeproj` in Xcode to explore the full source.

---

## 🔧 Requirements

- macOS with a recent version of **Xcode** installed.
- An iOS device or simulator with a reasonably up-to-date iOS version.
- An **Imgur API Client ID** for uploads.

---

## 🚀 Getting Started

### 1. Clone the repository

```bash
git clone https://github.com/ferrannl/Imgur-App-iOS.git
cd Imgur-App-iOS
